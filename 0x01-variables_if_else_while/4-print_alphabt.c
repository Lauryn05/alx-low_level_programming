#include <stdio.h>
/**
 *  main - Entry point
 *
 *  Return: 0 Always
 */
int main(){
	char ch;

	for(ch = 'a';ch <= 'z';ch++) {
		if(ch != e && ch != q){
			printchar(ch);
		}
	}
printchar('\n');

return (0);
}
