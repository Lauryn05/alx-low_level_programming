#include <stdio.h>
/**
 * main -Entry
 *
 * Return: 0 always
 */
int printf(const char *format, ...);

int main(void)
{
	printf("%s%s%s\n", "with proper grammar, ", "but the outcome is a piece of art,", "");
	return (0);
}
